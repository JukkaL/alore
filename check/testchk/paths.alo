module testchk

import os


-- Note that the paths are relative to test/ (since the test driver test.alo
-- is located there).


const Prefix = Join('..', 'check')

-- Location of test data files such as test case descriptions.
const TestDataPrefix = Join(Prefix, 'testchk', 'data')

-- Temp directory used for the temp files created when running test cases.
const TestTempDir = Join(Prefix, 'tmp')
